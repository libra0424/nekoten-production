<% if @user.followers.empty?%>
  フォロワーはいません
<%end%>
<% @user.followers.each do |user| %>
  <table>
    <tr>
      <td> <%= user.username %> </td>
      <td><%= link_to '詳細', user_path(user) %></td>
    </tr>
  </table>
<%end%>